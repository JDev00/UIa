package uia.application.list;

import uia.core.basement.Callback;
import uia.core.ui.View;

/**
 * Callback invoked when a new View is added to a list view.
 */

public interface OnViewAdded extends Callback<View> {
}
