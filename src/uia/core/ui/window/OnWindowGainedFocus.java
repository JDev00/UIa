package uia.core.ui.window;

import uia.core.basement.Callback;

/**
 * Callback invoked by a Window when the Window itself gains focus.
 * <br>
 * It returns the Window that got the focus.
 */

public interface OnWindowGainedFocus extends Callback<Window> {
}
